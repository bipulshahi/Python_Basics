#in cmd >> pip list >> list out all installed packages

#to install any package >> pip install package_name

import camelcase
c = camelcase.CamelCase()

txt = "hello world"
print(c.hump(txt))


#to uinstall a package >> pip unnstall package_name
